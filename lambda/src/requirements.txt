# AWS Lambda Contact Form Processor Dependencies
# Python 3.13 runtime

# AWS SDK for Python
boto3>=1.34.0

# Note: json, logging, os, datetime are built-in Python modules
# Note: botocore is included with boto3
